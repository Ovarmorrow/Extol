import {
  products
} from "./products";
export class response {
  numberOfProducts ? : any;
  products ? : products[];
  categories ? : any;
  brands ? : any;
  sellers ? : any;
  priceRanges ? : any;

}
