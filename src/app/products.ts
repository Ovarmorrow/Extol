export class products {
  data ? : any;
  brand ? : any;
  bv ? : any;
  cashback ? : any;
  ciPoints ? : any;
  description ? : any;
  ibv ? : any;
  id ? : String;
  image ? : any;
  links ? : any;
  maximumPrice ? : any;
  minimumPrice ? : any;
  name ? : String;
  referralUrl ? : any;
}