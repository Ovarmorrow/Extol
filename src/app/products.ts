export class products {
  public brand ? : any;
  public bv ? : any;
  public cashback ? : any;
  public ciPoints ? : any;
  public description ? : any;
  public ibv ? : any;
  public id ? : String;
  public imageUrl ? : any;
  public links ? : any;
  public maximumPrice ? : any;
  public minimumPrice ? : any;
  public name ? : String;
  public referralUrl ? : any;

  // brand = data.brands;

  // // bv = ;

  // cashback = data.;

  // ciPoints = ; Based on product, How Do?

  // description = ;

  // ibv = ;

  // id ? : String;

  // imageUrl = ;

  // links = ;

  // maximumPrice = ;

  // minimumPrice = ;

  // name ? : String;

  // referralUrl = ;
}
