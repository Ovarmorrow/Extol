export class products {
  brand ? : any;
  bv ? : any;
  cashback ? : any;
  ciPoints ? : any;
  description ? : any;
  ibv ? : any;
  id ? : String;
  imageUrl ? : any;
  links ? : any;
  maximumPrice ? : any;
  minimumPrice ? : any;
  name ? : String;
  referralUrl ? : any;

}
